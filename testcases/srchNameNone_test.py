# NOTE: Generated By HttpRunner v3.1.6
# FROM: testcases/srchNameNone.yaml


from httprunner import HttpRunner, Config, Step, RunRequest, RunTestCase


class TestCaseSrchnamenone(HttpRunner):

    config = Config("PhoneSearch").base_url("${ENV(BASEURL)}")

    teststeps = [
        Step(
            RunRequest("searchByidAndName")
            .with_variables(**{"a": None, "b": "Amili"})
            .get("/searchPhone")
            .with_headers(**{"UContent-Type": "application/json"})
            .with_json({"a": "$a", "b": "$b"})
            .validate()
            .assert_equal("status_code", 200)
            .assert_equal("body.Error_code", 0)
        ),
    ]


if __name__ == "__main__":
    TestCaseSrchnamenone().test_start()
